[phases.setup]
nixPkgs = ["nodejs", "chromium", "nss", "nspr", "atk", "at-spi2-atk", "cups", "libdrm", "libxkbcommon", "libXcomposite", "libXdamage", "libXrandr", "gbm", "alsa-lib", "pango", "cairo"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm run start"
